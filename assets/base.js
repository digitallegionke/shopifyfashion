// Theme JavaScript
const theme = {
  init() {
    this.mobileMenu();
    this.productGallery();
  },

  mobileMenu() {
    // Mobile menu logic
  },

  productGallery() {
    // Product gallery logic
  }
};

// Initialize theme
document.addEventListener('DOMContentLoaded', () => {
  theme.init();
}); 